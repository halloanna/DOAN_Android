<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.manager.doan_android">

    <application
        
        android:allowBackup="true"
        android:icon="@drawable/icon_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".bao_cao" />
        <activity android:name=".setting" />
        <activity android:name=".themmoi_tienchi" />
        <activity android:name=".themmoi_tienthu" />
        <activity android:name=".activity_themmoi" />
        <activity android:name=".activity_tongquat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>